# Ralph Progress Log
Started: Sat Mar 14 11:44:48 EDT 2026
---

## Codebase Patterns
- Node.js workspaces used for monorepo (packages/* in root package.json)
- tsconfig.base.json at root; each workspace extends it with `"extends": "../../tsconfig.base.json"`
- Each workspace references `../shared` in tsconfig `references` for project references
- ESLint 9 flat config (eslint.config.mjs) with typescript-eslint
- Root scripts: `npm run typecheck`, `npm run lint`, `npm run format`
- All workspace packages are scoped under `@npmdex/`
- Node.js v24.14.0 (.nvmrc)
- Drizzle ORM for database schema definition and migrations in packages/shared
- Schema defined in `packages/shared/src/schema.ts`, types in `packages/shared/src/types.ts`
- Run `npm run db:generate` in packages/shared to generate migrations, `npm run db:migrate` to apply
- Must run `npm install` at root before `npm run typecheck` (hoisted node_modules)
- Shared DB connection via `createDb()` from `@npmdex/shared` (uses DATABASE_URL env var)
- Use `onConflictDoUpdate` for upsert patterns with Drizzle ORM
- npm registry APIs: registry.npmjs.org for package info, replicate.npmjs.com/_changes for change feed, api.npmjs.org/downloads for download counts
- Run `npm run format:fix` before committing to fix Prettier formatting
- GitHub API: use `parseGitHubRepo()` from `packages/index-service/src/github.ts` for URL parsing
- index-service CLI flags: `--full-sync`, `--enrich-github`, `--max-packages N`

---

## 2026-03-14 - US-001
- Set up monorepo with Node.js workspaces and 5 packages: shared, index-service, score-service, api, web
- Created tsconfig.base.json with ES2022/Node16 module settings, composite project references
- ESLint 9 flat config with typescript-eslint, Prettier with shared config
- Files changed: package.json, tsconfig.base.json, eslint.config.mjs, .prettierrc, + 15 files in packages/*/
- **Learnings for future iterations:**
  - Use `tsc --build packages/*/tsconfig.json` for cross-workspace typecheck
  - ESLint 9 uses flat config (eslint.config.mjs), not .eslintrc
  - Each workspace has `@npmdex/shared` as a dependency for shared types
---

## 2026-03-14 - US-002
- Defined PostgreSQL schema using Drizzle ORM with 3 tables: packages, package_metadata, package_scores
- Created typescript_support enum type ('native', 'definitely-typed', 'none')
- Generated initial migration via drizzle-kit (drizzle/0000_damp_landau.sql)
- Exported Drizzle inferred types (Package, NewPackage, PackageMetadata, etc.) from packages/shared
- Files changed: packages/shared/src/schema.ts (new), packages/shared/src/types.ts (new), packages/shared/src/index.ts, packages/shared/package.json, packages/shared/drizzle.config.ts (new), packages/shared/drizzle/* (new)
- **Learnings for future iterations:**
  - drizzle-kit generate creates SQL migrations from schema changes
  - Drizzle config is at `packages/shared/drizzle.config.ts` with DATABASE_URL env var
  - `InferSelectModel` and `InferInsertModel` from drizzle-orm to derive types from schema
  - Root `npm install` must be run first to get typescript in node_modules for typecheck
---

## 2026-03-14 - US-003
- Built npm registry crawler in packages/index-service with full and incremental sync support
- Created shared DB connection utility (`packages/shared/src/db.ts`) using drizzle-orm + node-postgres
- Crawler uses CouchDB `/_changes` feed from replicate.npmjs.com for discovering packages
- Fetches package metadata from registry.npmjs.org and weekly downloads from api.npmjs.org
- Stores data in packages and package_metadata tables via upserts (onConflictDoUpdate)
- 100ms delay between requests to respect rate limits
- Sequence tracking via `.last-seq` file for incremental sync resume
- CLI supports `--full-sync` and `--max-packages N` flags
- Files changed: packages/index-service/src/index.ts, packages/index-service/src/crawler.ts (new), packages/index-service/src/registry.ts (new), packages/index-service/src/state.ts (new), packages/index-service/package.json, packages/shared/src/db.ts (new), packages/shared/src/index.ts
- **Learnings for future iterations:**
  - Node.js 24 has native `fetch` — no need for node-fetch dependency
  - npm registry package JSON can be very large (includes all versions); for future optimization, use `Accept: application/vnd.npm.install-v1+json` header for abbreviated metadata
  - Repository URL in registry data comes in various formats (git+https://, git://, .git suffix) — needs normalization
  - CouchDB `/_changes` feed returns design docs (prefixed `_design/`) that should be skipped
  - `tsx` is used to run TypeScript directly in index-service (`node --import tsx src/index.ts`)
---

## 2026-03-14 - US-004
- Built GitHub metadata enrichment pipeline in packages/index-service
- Created `github.ts` — GitHub REST API v3 client with auth (GITHUB_TOKEN), rate limit checking, and repo info fetching
- Created `enrich-github.ts` — enrichment pipeline that reads packages with repo URLs from DB, parses GitHub owner/repo, fetches stats, and writes to package_metadata table
- Updated `index.ts` CLI to support `--enrich-github` flag (combinable with `--max-packages`)
- Handles non-GitHub repos (skipped), inaccessible repos (null values), and rate limits (stops gracefully)
- Files changed: packages/index-service/src/github.ts (new), packages/index-service/src/enrich-github.ts (new), packages/index-service/src/index.ts
- **Learnings for future iterations:**
  - GitHub API `pushed_at` field represents the last push date (used as last commit date proxy)
  - GitHub API `open_issues_count` includes both issues and PRs — no separate count available without pagination
  - Use `parseGitHubRepo()` from github.ts to extract owner/repo from various URL formats
  - Rate limit check via `/rate_limit` endpoint before starting batch operations
  - `--enrich-github` flag runs enrichment instead of npm crawl
---
